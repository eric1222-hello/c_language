#include <iostream>

using namespace std;

int main()
{
    int n,t;
    while(cin>>n>>t)
    {
        int i,j,tmp;
        int num[t]={};

        for(i=0;i<t;i++)
        {
            cin>>tmp;
            num[i] += tmp;
        }
    }
}
